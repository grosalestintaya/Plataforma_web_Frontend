[phases.setup]
nixPkgs = ["nodejs_18", "npm-10_x"]

[phases.install]
cmds = [
  "npm cache clean --force",
  "rm -rf node_modules package-lock.json",
  "npm install"
]

[phases.build]
cmds = ["npm run build"]